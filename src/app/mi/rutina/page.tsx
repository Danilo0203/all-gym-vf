import { ClientRoutineScreen } from "@/features/client/components/client-routine-screen";

export const metadata = {
  title: "Mi rutina",
};

export default function ClientRoutinePage() {
  return <ClientRoutineScreen />;
}
